# ===== RPC CONFIGURATION =====
# Base Network RPC URL (get your API key from Infura, Alchemy, or use a public RPC)
# Example: https://base-mainnet.infura.io/v3/YOUR_API_KEY
# Or use a public RPC: https://base-mainnet.publicnode.com
BASE_RPC_URL=https://base-mainnet.infura.io/v3/YOUR_INFURA_API_KEY

# ===== COST MODEL CONFIGURATION =====
# Gas cost (USDC equivalent) per transaction on each DEX
UNISWAP_GAS_FEE_USDC=0.004
AERODROME_GAS_FEE_USDC=0.005

# Aerodrome trading fee in basis points (1 bps = 0.01%)
# Example: 1 = 0.01%
AERODROME_FEE_BPS=1

# ===== ARBITRAGE SIMULATION CONFIGURATION =====
# Trade size for arbitrage simulation (in cbBTC, human-readable units)
# Example: 0.1 = 0.1 cbBTC per trade (~$9,000 at current prices)
# Profit scales with trade size: larger trades capture more spread
ARB_TRADE_SIZE_CBBTC=0.1
AERODROME_POOL_ADDRESS=0x4e962bb3889bf030368f56810a9c96b83cb3e778

# ===== MONITORING THRESHOLDS =====
# Minimum price change (in USDC) to trigger logging
# Default: 0.01 (only log when price changes by at least $0.01)
PRICE_CHANGE_THRESHOLD=0.01

# Minimum spread percentage to log (optional)
# Default: 0.0 (log all spreads)
# Example: 0.1 would only log when spread > 0.1%
SPREAD_LOG_THRESHOLD=0.0

# ===== FEE CONFIGURATION =====
# Fixed transaction fees in USDC per transaction on each DEX
# These are flat fees deducted from each swap transaction
# Adjust based on actual DEX trading fees and UI fees
UNISWAP_TX_FEE_USDC=0.01

# Aerodrome Slipstream transaction fee in USDC per transaction
AERODROME_TX_FEE_USDC=0.01

# ===== ARBITRAGE SIMULATION CONFIGURATION =====
# Trade size for arbitrage simulation (in cbBTC, human-readable units)
# Example: 0.1 = 0.1 cbBTC per trade (~$9,000 at current prices)
# Profit scales with trade size: larger trades capture more spread
